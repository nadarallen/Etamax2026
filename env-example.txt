# ==========================================
# ETAMAX CONFIGURATION TEMPLATE
# Rename this file to .env.local and fill in the values
# ==========================================

# 1. Database (MongoDB)
# Connection string for your MongoDB Atlas or local instance
MONGODB_URI=mongodb://localhost:27017/etamax

# 2. Authentication (JWT)
# Random string for signing tokens. Generate with: `openssl rand -hex 32`
JWT_SECRET=super_secret_jwt_key_please_change_this

# 3. Payment Gateway (Razorpay)
# Get these from Razorpay Dashboard -> Settings -> API Keys
RAZORPAY_KEY_ID=rzp_test_placeholder_id
RAZORPAY_KEY_SECRET=rzp_test_placeholder_secret

# Secret used to verify Webhook signatures (Settings -> Webhooks)
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret

# Public Key for Frontend (Must match RAZORPAY_KEY_ID)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_placeholder_id

# 4. Security & Rate Limiting (Upstash Redis)
# Create a free Redis at https://console.upstash.com/
# Required for Middleware Rate Limiting
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token

# 5. Background Jobs (Cron)
# Secret required to trigger /api/cron/cleanup securely
CRON_SECRET=your_cron_secret_key

# 6. Global Config
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 7. Email Configuration (Nodemailer)
# Gmail or SMTP credentials for sending registration receipts
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
